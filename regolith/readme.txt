Regolith setup instructions:

Install regolith (see: https://regolith-desktop.com/)
Install picom with `sudo apt install regolith-compositor-picom-glx`
Move the configuration files into the proper directories, (as noted in
their headers).
