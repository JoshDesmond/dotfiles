# Bash scripts in this repository

When creating or updating Bash scripts under `bash/scripts/`, adhere to the standards in `bash/scripts/readme.txt`. That file is the source of truth for conventions (help headers, `--help` / `-h`, arguments, naming, and avoiding a separate command index).
